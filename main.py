from app.flash_card_app import FlashcardApp

def main():
    """
    Application entry point for the flashcard trainer.

    This module initializes and starts the application by creating
    an instance of the FlashcardApp controller. It contains no
    application logic, user interface code, or persistence logic.

    The module is intended to be executed as the main entry point,
    either directly or via `python -m`.
    """
    FlashcardApp()

if __name__ == "__main__":
    main()

